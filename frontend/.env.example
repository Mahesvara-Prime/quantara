# Copier vers `.env` pour activer les appels API réels (voir `src/shared/api/integration.ts`).
# En production, utiliser l’URL publique du backend (rebuild après modification).
# VITE_API_BASE_URL=http://127.0.0.1:8000
